export enum userGender{
    MALE = 'male',
    FEMALE = 'female',
}

export enum userProvider{
LOCAL = 'local',
GOOGLE = 'google',
FACEBOOK = 'facebook',
}

export enum userRole{
    USER = 'user',
    ADMIN = 'admin',

}
